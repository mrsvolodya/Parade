import { PaginationType } from "../types/PaginationType";

export const DEFAULT_PAGINATION: PaginationType = {
  currentPage: 1,
  itemsPerPage: 10,
  numberOfPages: 1,
  totalItemsCount: 0,
};